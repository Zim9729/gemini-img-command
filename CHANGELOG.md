# 更新日志 / Changelog

本文件记录 gemini-img-command 的版本变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.5.3] - 2026-09-05

### 修复（额度到点后自动续跑）

- **锁定绝对截止时间**：等待中读取到真实重置时间后，固定在该时刻加 15 秒重试。缓存到点过期不再退回原来的 30 分钟兜底；移除每轮至少等待 60 秒造成的截止时间漂移。
- **阻止旧时间滚到明天**：用量页保持打开时，重复读取相同的过期时刻不再将其更新为第二天。页面展示新的重置时刻时仍可更新等待目标。
- **续跑状态可核对**：诊断显示重置日期、实际等待截止时间和队列状态；到点输出自动重试日志。队列停止、重置或替换时退出等待。

## [2.5.2] - 2026-09-05

### 修复（用量页真实重置时间）

- **限额等待不再只按聊天回复或固定间隔猜测**：Gemini 的聊天页限额提示可能不含时刻，也可能显示与「查看用量」页不同的时间。脚本现在会在用户打开「查看用量」页时，读取并持久化**当前用量**卡的「重置时间」（例如 `20:17`），限额时优先按该绝对时刻等待；明确排除「每周限额」的重置时间，避免把两个额度混用。
- **等待期间可校正**：若队列已因没有明确时间而按默认间隔等待，用户随后打开「查看用量」页，脚本会在最多 5 秒内切换为真实重置时刻，无须等完旧的 30 分钟间隔。真实时刻未获取、过期或异常时保留既有的“回复解析 → 面板间隔”兜底链路。

## [2.5.1] - 2026-09-04

### 修复（据 v2.4.2 诊断反馈：同一对话框累积多张附件）

用户诊断显示输入区同时挂着 **4 个附件**（4 个「关闭附件」按钮 + 4 张输入框预览大图）：某张图附加成功但后续步骤失败/超时后，残留附件清不掉，下一张再附加就叠在一起，发送时一次带出多张图——即「有时候会有多张图片在同一个对话框」。

- **根因**：Google 把附件移除键的 aria-label 从「移除/删除」改成了**「关闭附件」**（图标 close），`clearAttachments` 的选择器全部落空，残留附件永远清不掉。
- **修复**：选择器补齐「关闭附件 / 移除附件 / 删除附件 / Close attachment / Remove file / Remove attachment」；新增兜底——输入区内**图标名为 close 的可见按钮**即附件移除键（label 再改也不怕）；清理轮数 5 → 10。

## [2.5.0] - 2026-09-04

### 新增

- **尺寸对齐原图**：Gemini 输出的分辨率常与上传原图不同（如 1024×512 vs 原图 1429×714）。开启「尺寸对齐原图」（面板开关，**默认开**）后，保存前自动把生成图缩放为与原图相同的像素尺寸。设计取舍：
  - **先在内存中缩放、再保存**，而非「保存后再改」：浏览器脚本无法改写已落到下载目录的文件，内存中一次成型保证只落盘一个尺寸正确的文件，并复用现有 canvas 编码管线（`createImageBitmap` → canvas 高质量缩放 → 按目标扩展名编码）；
  - 尺寸相同不处理；**比例不同（如 2:1 原图对 1:1 生成图）会拉伸对齐**并输出 ⚠ 提示；
  - GIF 不缩放（canvas 转码会丢动画）；目标格式 canvas 无法编码（如 `.bmp` 原图）时跳过；缩放失败按生成图原始尺寸保存并 ⚠ 提示，不中断队列；
  - 尺寸对齐关闭时零开销（不解码原图）。

## [2.4.2] - 2026-09-04

### 修复（据用户反馈：图已生成但流程卡住不走）

- **完成判定等不到「发送键恢复」**：发送后编辑器被清空，新版界面可能把发送键**置灰（disabled）**留在原位；完成判定原先要求发送键「可用」，置灰状态永远等不过 → 每张图都白等满单张超时（默认 5 分钟），表现正是「Gemini 已出图、脚本不动」。完成判定改为只要求发送键**存在且可见**（含置灰状态，`sendButtonVisible`）；「点发送」仍要求可用（`findSendButton`）。「已开始生成」判定同步改用存在性判定。
- **长等待可观测**：生成等待期间每 60 秒输出心跳日志（「仍在等待生成…已等 X 秒」），进入 30 秒取图窗口时输出提示日志——面板日志不再长时间零输出，排查「卡住」可直接看最后日志停在哪个阶段。

## [2.4.1] - 2026-09-04

### 修复（据 v2.4.0 诊断反馈：发送按钮 ❌、其余关键元素全部 ✅）

诊断确认回复容器选择器全部有效（`model-response=2` 等，回复区 2 张生成图也正确归类），真正卡住流程的是**发送按钮**：Google 改掉了发送键的 class / aria-label / 图标名，主选择器全数落空，每张图报「找不到发送按钮」。

- **发送按钮定位新增结构兜底**：主选择器与图标名全失效时，取输入区内**最右侧**的可用动作按钮（发送键是输入区末尾按钮的固有布局），排除停止键与麦克风/上传/添加附件类左侧工具按钮，避免误点听写或附件入口。
- **诊断升级（定位发送键改版）**：发送按钮未找到时同时报告 `button.send-button` 原始匹配数（>0 = 按钮在但被禁用/隐藏，如输入框为空时属正常现象）；新增「输入区按钮清单」——逐个列出输入区按钮的 label / 图标名 / 禁用与隐藏状态 / 位置，发送键再改名也一眼可辨。

## [2.4.0] - 2026-09-04

### 修复（Google 改版兼容，据用户「🔍 诊断」反馈）

用户反馈：页面上 12 张生成大图均以 `blob:https://gemini.google.com/…` 的 `<img>` 渲染，而脚本逐张报「回复中没有生成图片」——回复容器选择器（`model-response` 等）已不匹配新 DOM，图就在页面上却抓不到。

- **回复图抓取改为双路径**：主路径（回复容器选择器）不变；容器一无所获时启用与结构无关的兜底——只收集「DOM 位置在最后一条用户消息之后、且 src 是发送前全页快照中不存在」的大图。历史回复的图、用户消息里回显的原图（与发送前输入框预览同 blob 地址）、输入框预览由此自动排除。容器正常时走不到兜底，既有精确性（skipCount/skipEls 水位）不受影响。
- **完成判定兜底**：容器失效时「页面签名」恒定，「稳定 4 秒」会在发送按钮一恢复就误判完成（图可能还没渲染）。此时改用全页大图 src 集合参与签名，生成图渲染进来会让稳定计时重新起算。
- **「新对话」判定兜底**：容器失效时 `countResponses()` 恒为 0，`isFreshChat` 会把有历史消息的对话误判为新对话而跳过切换；现在回复容器为 0 且页面上仍有用户消息节点时不再误判。

### 变更

- **诊断工具升级（反馈即所得）**：
  - 首行新增「关键元素汇总」：输入框 / 发送按钮 / 新对话按钮 / 回复容器四项一行 ✅/❌——反馈被截断时结论也不丢；
  - 「页面大图」按所在区域归类计数（回复区 / 用户消息 / 输入框 / **未识别区域⚠**——最后一项占比高即坐实回复容器改版），并列出最近 5 张的区域、协议、尺寸、地址；
  - 新增「回复容器候选计数」（`model-response` / `response-container` / `message-content` / `chat-window` / `conversation-container` 等——主选择器归零时哪个候选仍有计数，新容器的名字就是哪个）；
  - 诊断结果改为**弹窗展示 + 「📋 一键复制」**：textarea 全文呈现，复制走剪贴板 API（失败回退 `execCommand`），成功后按钮变「✅ 已复制」；点遮罩或「关闭」退出。替代无法选中复制的 `alert()`。

## [2.3.3] - 2026-09-04

### 变更（扩展元数据与用户脚本对齐）

- **manifest 权限与 `@connect` 完全对齐**：`host_permissions` 此前只有 `https://*.域名/*`（仅子域），而 `@connect 域名` 允许裸域 + 子域。补齐 4 个裸域模式（google.com / googleusercontent.com / gstatic.com / googleapis.com），消除扩展版比油猴版少覆盖裸域的语义差异（生成图实际都在子域上，属纯对齐，无功能影响）。
- **manifest 描述与 `@description` 同步**：扩展描述此前是旧版文案；现在 `build-extension.ps1` 构建时自动从用户脚本头同步描述，连同版本号一道杜绝两处漂移。
- 用户脚本逻辑零改动（仅版本号字面量）。

## [2.3.2] - 2026-09-04

### 修复（代码审查）

- **下载无超时可致队列永久卡死**：`gmFetchBlob` 的 `GM_xmlhttpRequest` 未设超时，连接挂起（stalled 但未断开）时 Promise 永不 settle——此时已越过所有等待守护、心跳照常、锁一直持有，队列零进展。补 `timeout: 60 秒` + `ontimeout`，按单张失败进入正常记错流程；扩展版同步补齐（gm-shim 转发超时并在 background 无响应时触发 `ontimeout`，background fetch 加 `AbortSignal.timeout` 中止）。
- **跨标签页停止在生成等待期间不生效**：其他标签页写入的 `stopped` 此前要等当前张超时（默认 5 分钟）后才在循环顶部被发现。`sendAndWait` 全程轮询存储（1 秒粒度），停止/替换队列即时以 `stoppedByOther` 中断当前张、回退 `pending` 不记错误；限额等待同样补上 `stopped` 检查（原先只看队列替换，跨标签页停止要白等满 30 分钟～24 小时）。
- **文件名 chip 子串碰撞**：`includes(nameKey)` 会把残留 chip「sunset-2024-v2.jpg」误判为当前文件「sunset-2024」的附件。三处匹配（`attached` / `chipAttached` / `verifyAttachment`）统一改为整体匹配（前后非文件名字符），并放行 chip 截断显示（nameKey + 省略号）。
- **长对话虚拟滚动误报**：同对话续跑且对话很长时，旧回复被移出 DOM 使位置水位失真，导致误报「回复中没有生成图片」。水位失真时退化为纯身份比对（快照集合）；完成签名（页面稳定 4 秒判定）只统计本次新增回复；started 判定兼用身份比对。
- **严格原名模式下 `.bmp` 名实不符无提示**：canvas 无法编码 bmp，`convertBlob` 原样返回导致 PNG/WebP 数据存进 `.bmp` 且无任何提示（GIF 分支有提示）。现与 GIF 一致明确 ⚠ 日志。
- **`processQueue` 无顶层 catch**：per-item try 之外的异常以未处理 rejection 逃逸（所有调用点均 fire-and-forget），表现为无声停止。补顶层 catch 记日志 + toast，当前张保持 pending 由刷新续跑重试。
- **旧版对象形式 cfg 静默重置**：`safeParse` 放行对象值（`GM_getValue` 支持结构化克隆，旧版直接存对象时 `JSON.parse(对象)` 抛错回退 `{}` 丢配置）。
- **`fileSweep` 删不掉非数字键**：按原始键删除 `file:*` 残留（原实现 parseInt 得 NaN 后拼出 `file:NaN` 删空），顺带清理非规范数字键。
- **若干小项**：`SCRIPT_VERSION` 运行时取 `GM_info.script.version`（消除与 `@version` 双写漂移，诊断面板死分支一并移除）；多条 toast 依次向下堆叠不互相遮盖；面板拖动位置持久化（页面移除重建/刷新后恢复，钳制在视口内）。

## [2.3.0] - 2026-09-04

### 新增

- **用量限额检测与自动等待**：Gemini 免费额度用尽时回复纯文字（如「一旦您的额度重置，我就可以创建更多图片。请在"设置"中查看您的使用情况。」），脚本会在「无图回复」的等待窗口内扫描本次新增回复的文本，命中中英文限额表述（额度/限额/配额/用量/上限/quota/rate limit/usage limit/daily limit/once your limit/create more images 等）即进入限额模式：
  - 当前图回退 `pending`（不记错误、不删除排队文件），面板显示 ⏰；
  - 等待时长优先从回复文本解析明确时间（「X 小时/分钟后」「14:30」「下午 3 点」「in 2 hours」等中英文表述，钳制 60 秒 ~ 24 小时），回复不含时间则退回面板间隔（默认 30 分钟，「限额等待(分)」输入框 1–1440 可调）；
  - 到点自动重试该图，额度恢复后队列无缝继续；重试期间心跳与多标签页锁保持；
  - 等待可被「停止」「重置」随时打断；其他标签页重置/替换队列时 5 秒内自动退出等待。

### 修复（发布前自查）

- **限额分支的保存防护**：限额时保存快照原本既不做「队列被替换」检查也不合并「跨标签页停止」标志（两道防护此前只存在于循环底部的保存点），会把新队列碾碎、或抹掉停止标志后无视停止继续跑。现在与循环底部一致：替换 → 不保存退出；停止 → 合并标志并短路。
- **限额等待中被停止的项无法续跑**：`'quota'` 状态此前不计入 `hasPending`，停止后点「执行」会跳过续跑分支、静默丢弃该张。现 `updatePanelState` / `executeClick` 的未完成判定都包含 `'quota'`；且存储中该张实际保存为 `'pending'`（⏰ 仅为等待期间的显示态），刷新/崩溃后按待处理恢复。

## [2.2.0] - 2026-09-04

### 新增

- **「🗑 重置」功能**：面板按钮行与油猴菜单各一个入口，一键清除当前队列（localStorage meta + IndexedDB 排队文件）、中断正在执行的循环、清空本次已选文件与日志。提示词、选项、记住的文件夹**保留**。面板按钮两步确认（首击变红「⚠ 确认重置？」，4 秒内再击生效），防误触。
- 实现上写入「墓碑 meta」：id 与任何运行中队列不同且 `done=true`，各标签页的 `processQueue` 在既有检查点识别出队列被替换而退出，且不会用旧快照回写存储——已重置的队列不会被残留循环复活。

### 修复

- **队列收尾交接**：`processQueue` 退出收尾期间若出现了新的待处理队列（重置后立即点「执行」等场景），新调用会因 `localRunning` 被挡住而挂起、需刷新页面才续跑。`finally` 中检测到「id 不同的新待处理队列」时自动接手（锁竞争失败/队列已停止或完成时不触发，无自旋风险）。

## [2.1.7] - 2026-09-04

### 修复

- **跨标签页「停止」失效**：`processQueue` 保存进度用的是循环开始时的内存快照，会覆盖其他标签页写入的 `stopped=true`，导致 B 标签页点停止后 A 标签页仍把剩余队列全部跑完。保存前先从 localStorage 合并停止标志。
- **「在当前对话继续」时存错图**：`collectResponseImages` 从后往前扫描全部回复，本条回复纯文字/被拒绝时会命中上一条历史回复的图，按当前文件名保存且状态标 ✅。改为只扫描发送后新增的回复；同时排除与用户消息同地址的「回显原图」（原先可能被存成 `_2` 副本）。
- **IndexedDB 泄漏 + 半启动状态**：新任务不清理上一轮残留的 `file:*` blob（更大的旧队列/中途放弃的队列永久占存储）。新增 `fileSweep` 在开新任务前清掉越界键；写入顺序改为「先清扫 → 写文件 → 最后写 meta」，避免两步之间崩溃留下「有队列无文件」的半启动状态（第一张就报文件数据丢失）。
- **上传途径无记忆**：页面开始忽略合成 paste/drop（如校验 `isTrusted`）时，每张图都要在失效途径上各白等 12 秒才落到 file input（50 张 ≈ 20 分钟纯浪费）。上次成功的途径存入 `cfg.uploadPath`，下次优先尝试。
- **重复附件风险**：某途径实际已成功但预览 DOM 检测失效时，原实现会继续尝试下一种途径把同一张图附加两遍。途径失败后先检查文件名 chip 再换途径；`verifyAttachment` 也改为只认「本次上传新增」的预览元素，上一张残留的旧预览不再误放行。
- **原生选文件夹后标签显示文件名**：`showDirectoryPicker` 的 paths 不含根目录名，标签显示的是排序后第一个文件名；改用目录句柄的 `name`。
- **GIF 名实不符**：GIF 数据（可能含动画）不做 canvas 转码（会丢成单帧）是对的，但非严格模式下扩展名也应跟随数据改为 `.gif`；严格模式下保留原文件名并明确提示内容与扩展名不符。
- **同名文件互相覆盖**：不同子文件夹里的同名文件（`a/1.jpg`、`b/1.jpg`）下载互相覆盖，追加 ` (2)` 序号区分。
- **生成完成提前误判**：完成判定在「有发送按钮且无停止按钮」之外，增加「页面签名（回复数量、末条回复文本长度、回复图片地址）连续 4 秒无变化」，防止改版后发送按钮不禁用、停止按钮不出现时过早判完，把仍在生成（>30 秒）的回复误报为「没有生成图片」。
- **续跑静默使用旧提示词**：面板提示词改过后续跑仍用队列发起时的提示词，现在会明确 toast + 日志提示「面板修改不生效」。
- **面板重建累积 window 监听器**：拖动的 mousemove/mouseup 改为拖动期间挂载、松手即摘除，不再随面板重建无限累积。
- **旧格式/损坏 meta 导致按钮无响应**：`metaLoad` 统一规范化（补齐/重建 statuses/idx/errors/prompt），`m.statuses.some(...)` 不再抛 TypeError（async 点击处理器无 catch，此前表现为点击无响应、只剩控制台报错）。
- **多标签页锁竞态**：获锁改为「写后回读确认 + 250ms 后复核」，两个标签页同一毫秒双双获锁时，后写者独占、先写者自动退出。
- **文件选择框拦截误判**：macOS 等平台的系统对话框不触发 window blur，原 2.5 秒无 blur 即判「被拦截」会误杀真实弹出的选择框、又叠加按钮面板（双重弹窗）。改用 `document.hasFocus()` 兜底判定对话框已打开。
- **uploadSettle 被无关 spinner 拖慢**：上传进度条等待范围从全文档收窄到输入区（composerRoot），页面上常驻的无关 spinner 不再让每张图白等满 120 秒超时。
- **队列被替换时旧快照覆盖新队列**：标签页 A 卡死后，用户在标签页 B「停止 → 重新执行」，A 苏醒后会把循环开始时的旧快照 `metaSave` 回去，碾碎 B 的新队列并按旧文件名处理新文件。每个队列新增代 ID（`meta.id`），`processQueue` 在每张的检查点、停止保存、完成保存处校验 id，不一致立即退出且不再写存储。
- **迟到回复被误认成当前结果**：`sendAndWait` 的发送前快照原本在「找发送按钮」的等待之前拍摄；同对话模式下，上一张超时后服务端仍在生成、其回复恰好在等待窗口内到达时，会被漏进快照当成当前张的结果存错文件。快照移到紧贴 `btn.click()` 之前。
- **文件名 chip 子串碰撞**：`sunset.jpg` 的 nameKey「sunset」会匹配残留 chip「sunset-2.jpg」，导致裸发提示词。chip 校验（`chipAttached` / `verifyAttachment` 条件 1）优先匹配完整文件名（含扩展名）。
- **发起任务失败无提示**：`executeClick` 末尾 `await startRun(...)` 无 catch，IndexedDB 写满等异常表现为点击无响应（未处理的 promise rejection）。补 catch 并 toast 报错原因。
- **`waitFor` 超时参数无兜底**：传入非数字时 `Date.now() - t0 >= NaN` 恒为 false、永不超时（潜在）。非法/缺省超时统一按 10 分钟兜底。
- **点面板「×」触发拖动**：标题栏 mousedown 冒泡自关闭按钮，关闭时面板可能被拖动几像素；按钮点击不再进入拖动逻辑。

### 变更

- 版本号 2.1.6 → 2.1.7。（说明：2.1.3–2.1.6 的变更此前未单独记录）

## [2.1.2] - 2026-09-03

### 修复

- **下载 `Failed to fetch`**：Gemini 常把生成图渲染为 `blob:` 地址并在加载后 `revokeObjectURL`，事后 `fetch` 该地址必然失败。改为三级取数：`fetch` → `GM_xmlhttpRequest` → **直接把 `<img>` 画到 canvas 导出**（同源 blob:/data: 不会污染 canvas），全部失败才报错并把各步原因拼在一起。
- **「严格原文件名」时真正对齐格式**：此前把 PNG 数据存成 `.jpg` 名仅打警告；现在用 `createImageBitmap` + canvas 按扩展名重编码（`.jpg` → JPEG 质量 0.95，`.png` → PNG，`.webp` → WebP），文件名与内容一致。

## [2.1.1] - 2026-09-03

### 修复

- **上传误判（根因）**：`uploadViaPaste` 用 `dispatchEvent` 返回值判断成败，而 Gemini 接管粘贴时会 `preventDefault` → 返回 `false` → 成功被当失败、1 秒内直接抛错、提示词与发送全被跳过。改为只要事件派发成功就进入 `attached()` 检测等待（最多 12 秒）。
- **附件检测改为全局识别预览元素**：此前只在 `composerRoot()` 内找新 `<img>`，而 Gemini 附件预览是 `rich-textarea` 的兄弟节点。改为全局找新出现的 `img[src^="blob:"]` / `uploader-file-preview` 等元素（排除回复区与本脚本面板）。
- **`composerRoot()` 范围扩大**：增加 `input-container` / `input-area-v2` / `.input-area-container`，发送按钮、附件删除按钮、上传进度条都能在正确范围内找到。
- **粘贴途径提到第一位**，file input 作为兜底（Gemini 是点「+」时才动态创建 file input）。
- **上传进度等待**改为只看非回复区的可见进度条，稳定等待延长到 1.5 秒，避免图片还在上传就点发送。
- 诊断新增「附件预览元素」计数，便于排查预览 DOM 结构变化。

## [2.1.0] - 2026-09-03

### 变更

- **彻底去掉整页刷新**：此前每张图找不到「新对话」按钮就 `location.assign('/app')` 整页刷新续跑，叠加其它问题导致刷新循环。改为：按钮 → SPA 路由软跳转（`history.pushState('/app')` + `popstate`）→ 都失败则在当前对话继续并记录警告，绝不刷新页面。相关 `freshChat` / `gic-nav` 刷新续跑机制一并删除。
- **「新对话」按钮识别扩大**：优先用 `data-test-id*="new-chat"` 组件与 `a[href="/app"]`，文案兜底改为 `/新(对话|聊天|会话)/` 与 `/new (chat|conversation)/i`，能匹配「发起新对话」。

### 修复

- **提示词输入三级兜底**：`execCommand('insertText')` → 模拟粘贴 `text/plain`（与图片粘贴同一通道）→ 直接写 DOM 并派发 `input` 事件；每级后重新定位编辑器并校验文本确实进入，成功才继续。
- **编辑器定位只取可见元素**：此前 `querySelector('.ql-editor')` 取 DOM 第一个，可能命中隐藏实例导致文字进不去；现在过滤 `getClientRects()`，取最下方的可见者。
- **每张开始前清掉残留附件**：失败后留在输入框里的图片 chip 会被移除，避免下一张叠加成两张；新对话步骤移进单张 try 块，失败只标记该张，不再中断整个队列。
- 全选改用 Selection API，避免 `execCommand('selectAll')` 在焦点不对时选中整页。
- 诊断新增「输入框候选/可见数量」，便于排查编辑器选择器。

## [2.0.2] - 2026-09-03

### 修复

- **`GM_getValue`/`GM_setValue` 加 localStorage 回退**：脚本在 IIFE 顶层同步调用 `GM_getValue`，是唯一能在打印横幅后、创建按钮前直接抛错终止整个脚本的位置（如 Greasemonkey 4 只提供异步 `GM.*`）。回退后不再因此中止。
- **`renderList` 的 `innerHTML` 改为 DOM API 构建**：Google 已下发 `require-trusted-types-for`（目前 Report-Only），一旦转正 `innerHTML = 字符串` 会抛 `TypeError` 中断 `processQueue`。改完后 `escapeHtml` 成死代码，一并删除。
- **跨域图片直接走 `GM_xmlhttpRequest`**：不再先用页面 `fetch` 触发 CORS 报错刷屏，仅 `blob:`/`data:`/同源地址用 `fetch`。
- 版本号 2.0.1 → 2.0.2，便于确认跑的是新文件而非缓存旧版。

## [2.0.1] - 2026-02-14

### 修复（可观测性与健壮性加固）

- **加载横幅**：脚本一运行就在 F12 控制台输出蓝色 `[gic] Gemini 批量图片生成面板 v2.0.1 已加载` —— 页面上没出现悬浮按钮时，可立即判断是「脚本没在运行」（控制台无横幅）还是「脚本在运行但界面异常」（有横幅无按钮）。
- **悬浮按钮提前显示**：不再固定等 1.5 秒，`document.body` 一就绪（约页面加载初期）立即创建 🖼 悬浮按钮与面板。
- **界面看门狗**：每 4 秒检查悬浮按钮是否仍在页面中，被页面移除时自动补回。
- **报错可见化**：启动/界面创建出错时弹出页面提示（此前只写控制台，用户无感知）。
- 悬浮按钮提示文字与面板标题栏显示当前版本号；`/诊断` 版本读取失败时回退到内置版本常量。

## [2.0.0] - 2026-02-14

### 变更（交互模式重构）

- **移除聊天命令形式**（`/img`、`/imgf` 等输入框命令不再存在），改为**配置面板**交互：
  - 右下角 🖼 悬浮按钮（或 `Alt+G`、油猴菜单）打开面板
  - 面板内提前配置：**选择文件夹** + **填写提示词**（自动保存）+ 选项（每图新开对话 / 严格原文件名）
  - 点击「▶ 执行」开始全自动循环，直至文件夹内全部图片完成
  - 「⏹ 停止」随时终止；「🔍 诊断」排查页面元素问题

### 新增

- **记住上次的文件夹**：文件夹句柄持久化到 IndexedDB，下次打开面板点「执行」直接使用（浏览器可能要求重新授权一次）。
- 提示词输入框实时保存，重启浏览器后仍在。
- 执行按钮状态与队列状态联动（执行中禁用「执行」、启用「停止」）。

## [1.2.0] - 2026-02-14

### 修复

- 修复「提示词弹窗之后，文件夹/文件选择框不再弹出」的问题：Chrome 要求系统选择框必须在**用户手势内**直接打开。现在改为**先弹选择框、之后再询问提示词**。
- 修复油猴菜单入口可能因缺少用户手势而静默失败的问题（改为页面内按钮兜底）。

### 新增

- 文件夹选择三层兜底：原生 `showDirectoryPicker()`（Chrome/Edge，体验最佳）→ `input[webkitdirectory]` 上传控件 → 页面内按钮面板（浏览器拦截时点击按钮打开，保证一定能选到）。
- 选择框被浏览器拦截时自动检测（2.5 秒未弹窗即判定）并切换为按钮面板。
- `/imgdiag` 新增：脚本版本、文件夹选择方式、userActivation 支持情况，便于远程排查版本问题。

## [1.1.0] - 2026-02-14

### 新增

- `/imgf <提示词>` 命令：直接选择**整个文件夹**，自动批量处理其中所有图片（递归含子文件夹，自动忽略非图片文件与隐藏文件）。
- `Alt+F` 快捷键与油猴菜单「📁 选整个文件夹批量处理」入口。
- 批量开始时显示文件夹名与加入队列的图片数量；超过 80 张时提示可分批处理（浏览器存储考虑）。
- 文件夹内图片按路径名排序（中文友好），处理顺序可预期。

## [1.0.0] - 2026-02-14

### 新增

- 首个正式版本。
- `/img <提示词>` 命令：在 Gemini 输入框直接触发「选图 → 上传 → 发送 → 等待生成 → 按原图文件名下载」全流程。
- `Alt+G` 快捷键与 Tampermonkey 菜单命令入口。
- 多图排队处理，每张图自动新开对话（`/imgconf` 可关）。
- 生成图严格按原文件名保存（`/imgname` 可切换为自动纠正扩展名）；一次多图时追加 `_2`、`_3` 后缀。
- 队列持久化（localStorage + IndexedDB）：中途刷新或关闭页面后自动断点续跑。
- 多标签页互斥锁（心跳保活），避免重复处理。
- 右下角可拖动状态面板：实时进度 + 运行日志。
- `/imgdiag` 诊断命令：检查输入框 / 发送按钮 / 上传入口 / 回复容器等关键元素，便于 Google 改版后排查。
- 上传双通道：页面 file input 优先，失败自动降级为编辑器模拟粘贴。
- 下载双通道：原生 fetch 优先，跨域受限时走 GM_xmlhttpRequest。
